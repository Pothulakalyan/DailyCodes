#include<bits/stdc++.h>
using namespace std;
int ProdIndividualNumbers(int n)
{
    ///base casse
    if(n%10==n) return n;
    return ProdIndividualNumbers(n%10)*ProdIndividualNumbers(n/10);
}
int main()
{
    int a;
    cin>>a;
    int k=sumofIndividualNumbers(a);
}

Small Concept
---------
N-- VS --N

(N--) means Pass The Value and Subtract second
(--N) Means Subtract first and pass the value

