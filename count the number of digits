#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n=10;
    //how many didigits in a number
    int l=log10(n)+1;
    cout<<l<<endl;
    //it gives the number of binary digits
    int k=log2(n)+1;
    cout<<k;
        
    
}
